spring:
  application:
    name: wechat-server

server:
  # 指定端口在20001
  port: 20001

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:20000/eureka