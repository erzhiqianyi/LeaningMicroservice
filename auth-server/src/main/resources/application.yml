spring:
  application:
    name: auth-server

server:
  # 指定端口在20002
  port: 20002

eureka:
  instance:
    hostname: 192.168.0.88
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://192.168.0.88:20000/eureka
